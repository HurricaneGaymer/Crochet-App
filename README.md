# Crochet-App
This is created solely on App Lab from code.org with JavaScript


Features
Adds the number and displays the count
Removes -1 when clicked to the minus count
Resets the count when clicked to 0 button
Saves project counts
Note tabs
Settings to change the sound and adjusts features for disability related
Has a log in page




First Step(8/14/2025 - 8/18/2025)
Get the main count button to add digits display on the screen
Code the minus button to change the count display to the previous digit
Code the reset button to reset the count display


Errors
- Had to adjust the label with a label rather than text to the display count, and having AI to help me out with the code, as I had no idea of how to use variables and JavaScript
- Time to learn JavaScript tomorrow
- When doing the reset code button, I got the output of false and made an extra variable function without realizing it
//Resetting the Count
onEvent("Reset", "click", function( ) {
  currentCount = currentCount == 0;
  setText("Count-Display", currentCount);
});

  So I changed that highlighted code to just 0






Second Step()
Make a login user screen
Add a code for the user login
Make a save project feature



Errors

